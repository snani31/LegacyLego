info:
  name: Get User Data By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{auth_host}}/admin/realms/{{realm}}/users/:user-id"
  params:
    - name: user-id
      value: ""
      type: path
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
