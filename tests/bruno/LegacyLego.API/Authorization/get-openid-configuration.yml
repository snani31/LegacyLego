info:
  name: Get OpenId Configuration
  type: http
  seq: 5

http:
  method: GET
  url: "{{auth_host}}/realms/{{realm}}/.well-known/openid-configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
