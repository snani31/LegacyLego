info:
  name: Test Secret Data
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/jwt/secret-data"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
