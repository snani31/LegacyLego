info:
  name: Get Active Order
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/orders/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
