info:
  name: Get Order History
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/orders/history?Cursor="
  params:
    - name: Cursor
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
